import 'package:flutter/material.dart';

class DocumentModel extends StatelessWidget {
  const DocumentModel({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
