services:

  switrans2-app-martin-argoty:
    build:
      context: .
      dockerfile: Dockerfile
      target: ${STAGE}
      args:
        - env=${ENV}
    container_name: switrans2-app-martin-argoty
    ports:
      - "3003:80"

